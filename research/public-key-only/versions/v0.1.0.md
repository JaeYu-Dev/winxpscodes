# v0.1.0 — PK-only baseline
Date: 2026-08-09

## Purpose
Freeze the research state after correcting the SP1/SP3 FIPS-path confusion and before beginning exact ADVAPI/KSecDD quotient-state analysis.

## Main corrections
1. OpenSSL 0.9.8h requests 64 bytes from CryptoAPI.
2. XP SP3 RSAENH does not follow the leaked SP1 20-byte FIPS186Gen loop verbatim for the observed runtime path; shipped SP3 behavior generates 40-byte blocks and serves 64 bytes as 40+24.
3. The SP1 SHA_mod_q 5-byte memcpy is a real source-level defect, but it does not by itself erase 120 bits of the current fresh RNG output because `rgdwNewSeed[5:20]` was already filled by NewGenRandom immediately before SHA_mod_q.
4. Provider-side easy-collapse hypotheses (SystemFunction036 failure ignored, caller buffer as sole entropy, simple length reuse) are falsified for the analyzed SP3 path.

## Positive reduction retained
Machine algebra for the RSAENH FIPS-186-style block shows a nominal 320-bit `(state20, aux20)` pair is projected through one effective 160-bit XVAL before generation of the 40-byte block. Therefore `|Image(out40)| <= 2^160`.

## Next theorem target
Let `R=(R0,...,Rm)` be the SystemFunction036 outputs consumed before the first Bitcoin private scalar. Bound the joint reachable image `|Image(R)|` under exact shipped SP3 ADVAPI/KSecDD state evolution. The high-value threshold is <2^128 or a structural algorithm beating generic ECDLP.

## Next machine-code target
ADVAPI32 SP3: selector/rekey path around `rc4_safe_select`, `RandomFillBuffer`, `GatherRandomKeyFastUserMode`, and the KSecDD `IOCTL_KSEC_RNG_REKEY` bridge. Specifically test whether eight nominal RC4-safe entries really create eight independent states or are deterministic projections of a much smaller recurrent root.
